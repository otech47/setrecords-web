import React from 'react';

import Base from './Base';

export default class Dashboard extends Base {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div id='Dashboard'>
                Dashboard
            </div>
        );
    }
}
